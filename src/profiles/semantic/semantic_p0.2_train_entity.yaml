seed: 42
scope:
  token_noise: [train]
  label_noise: []
token_noise:
  - name: semantic_noise
    params:
      p: 0.20 # fraction of tokens per sentence
      entity_strategy: "entities_only" # "protect", "entities_only", "all"
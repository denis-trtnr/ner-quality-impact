seed: 42
scope:
  token_noise: [train, validation, test]
  label_noise: []
token_noise:
  - name: semantic_noise
    params:
      p: 0.30 # fraction of tokens per sentence
      entity_strategy: "all" # "protect", "entities_only", "all"